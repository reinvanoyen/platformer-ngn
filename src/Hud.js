var Hud = Base.extend( {
	
	constructor: function()
	{
	},
	update: function()
	{
		// console.log( 'Velocity Y: ' + Game.world.active_objects.get( 'player' ).vy );
		// console.log( 'Velocity X: ' + Game.world.active_objects.get( 'player' ).vx );
	}
	
} );